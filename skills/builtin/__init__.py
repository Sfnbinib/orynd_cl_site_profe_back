"""Built-in skills shipped with ORYND.

Modules placed here are auto-discovered by ``SkillRegistry.load_builtin()``.
Each module should define exactly one ``Skill`` subclass with a unique ``slug``.
"""
